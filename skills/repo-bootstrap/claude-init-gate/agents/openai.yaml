# Skill-creator UI metadata convention. This filename is not an OpenAI-only claim.
interface:
  display_name: "Claude Init Gate"
  short_description: "Claude LLM-debuggable gate bootstrap"
  default_prompt: "Use $claude-init-gate to inspect this repo and set up a reviewed, check-only, LLM-debuggable make check quality gate with approval."

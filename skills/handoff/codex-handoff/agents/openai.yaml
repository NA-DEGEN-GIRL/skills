# Skill-creator UI metadata convention. This filename is not an OpenAI-only claim.
interface:
  display_name: "Codex Handoff"
  short_description: "Save and resume Codex handoff snapshots"
  default_prompt: "Use $codex-handoff to create or resume a compact repo-local handoff snapshot, including scoped per-task lanes."

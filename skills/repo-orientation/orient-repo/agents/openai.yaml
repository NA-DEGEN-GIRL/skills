# Skill-creator UI metadata convention. This filename is not an OpenAI-only claim.
interface:
  display_name: "Orient Repo"
  short_description: "Read-only orientation report for any repository"
  default_prompt: "Use $orient-repo to inspect this repository read-only and produce an orientation report."
